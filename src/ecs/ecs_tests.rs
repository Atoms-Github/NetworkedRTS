


#[cfg(test)]
mod tests {
    use crate::rts::systems::velocity_system::VelocitySystem;
    use crate::rts::systems::velocity_system::VelocitySystemTwo;
    use crate::ecs::ecs_manager::*;

    #[test]
    fn it_works() {
        let mut ecs_system = EcsManager::new();
        // let system = VelocitySystem{};
        // let system_two = VelocitySystemTwo{};
        // ecs_system.register_system(system);
        // ecs_system.register_system(system_two);
        // ecs_system.sim();

    }
}

